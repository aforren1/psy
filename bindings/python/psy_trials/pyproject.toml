[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "psy-trials"
version = "0.1.1"  # must equal psy_trials.h's PSYTR_VERSION_STRING; tests/test_trials.py checks it
description = "Trial sequencing for psychophysics: orders, constraints, interleaved adaptive tracks"
readme = "README.md"
requires-python = ">=3.8"
license = "MIT-0"

# The C extension is declared in setup.py (Extension) as psy.trials; this file
# just pins the build backend so `pip install .` works. `psy` is a PEP 420
# implicit namespace package: no __init__.py is shipped, so the other psy-*
# distributions install next to this one without either owning the namespace.

[tool.cibuildwheel]
# abi3 (Limited API): setup.py forces a cp38-abi3 wheel tag, so one build per
# platform serves every supported CPython. cibuildwheel >= 4 has no cp38 build
# interpreter, so build on cp39; the wheel is still cp38-abi3.
build = "cp39-*"
skip = "*-musllinux_*"
test-command = "python -c \"import psy.trials as pt; t = pt.Trials(n_conditions=3, reps=2, order=pt.ORDER_FULL_RANDOM, rng=1); print('ok', t.schedule())\""

[tool.cibuildwheel.macos]
archs = ["universal2"]
